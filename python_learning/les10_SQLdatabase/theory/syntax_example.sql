-- CREATE TABLE USERS(username TEXT NOT NULL PRIMARY KEY,
-- password NOT NULL, email NOT NULL);

-- INSERT INTO USERS (username, password, email)
-- VALUES ("john", "8234niuk4", "ojin@gmail.com")

-- SELECT * FROM USERS;

-- insert into users (username, password, email)
-- VALUES ("mika", "2766736", "uwgi@gmail.com"),
-- ("kirlo", "qih870", "qhio@boom.eu"),
-- ("vika", "ftykoi70", "uwgi@email.io")

-- SELECT * FROM users